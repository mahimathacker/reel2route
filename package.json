{
  "name": "reel2route",
  "version": "0.1.0",
  "private": true,
  "description": "Turn travel content into explainable, personalised trip plans.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names api,web --prefix-colors cyan,magenta \"npm run dev --workspace=@reel2route/api\" \"npm run dev --workspace=@reel2route/web\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
